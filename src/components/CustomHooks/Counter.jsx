// import { useRef } from "react";
// import useCounterOnView from "../CustomHooks/CustomHooks";

// const Counter = () => {
//   const counterRef = useRef(null); // কাউন্টার ট্র্যাক করার জন্য
//   const count = useCounterOnView(counterRef, 0, 100, 20); // ০ থেকে ১০০ পর্যন্ত

//   return (
//     <div ref={counterRef} className="text-center py-20 bg-gray-200">
//       <h2 className="text-4xl font-bold">Visitors: {count}</h2>
//     </div>
//   );
// };

// export default Counter;
